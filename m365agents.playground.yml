# yaml-language-server: $schema=https://aka.ms/m365-agents-toolkits/v1.11/yaml.schema.json
# Visit https://aka.ms/teamsfx-v5.0-guide for details on this file
# Visit https://aka.ms/teamsfx-actions for details on actions
version: v1.11

deploy:
  # Install development tool(s)
  - uses: devTool/install
    with:
      testTool:
        version: ~0.2.7
        symlinkDir: ./devTools/playground
      nodejs:
        symlinkDir: ./devTools/nodejs

  # Generate runtime environment variables
  - uses: file/createOrUpdateEnvironmentFile
    with:
      target: ./.env
      envs:
        CONNECTIONS__SERVICE_CONNECTION__SETTINGS__CLIENTID: ""
        CONNECTIONS__SERVICE_CONNECTION__SETTINGS__CLIENTSECRET: ""
        CONNECTIONS__SERVICE_CONNECTION__SETTINGS__TENANTID: ""
        CONNECTIONS__SERVICE_CONNECTION__SETTINGS__ANONYMOUS_ALLOWED: "True"
        AZURE_OPENAI_API_KEY: ${{SECRET_AZURE_OPENAI_API_KEY}}
        AZURE_OPENAI_DEPLOYMENT_NAME: ${{AZURE_OPENAI_DEPLOYMENT_NAME}}
        AZURE_OPENAI_ENDPOINT: ${{AZURE_OPENAI_ENDPOINT}}
        FUTURECOMPLETE_API_BASE_URL: https://inait-saas-apim-jjyzmt7v.azure-api.net
        FUTURECOMPLETE_DASHBOARD_URL: https://futurecomplete.inait.ai
        FUTURECOMPLETE_TRIAL_USERS_URL: https://api.forecasting.inait.ai/users/dev/users
        FUTURECOMPLETE_TRIAL_PLAN_ID: trial
        FUTURECOMPLETE_JOB_STATUS_PATH_TEMPLATE: /v1/sessions/{session_id}/status
        FUTURECOMPLETE_JOB_RESULT_PATH_TEMPLATE: /v1/sessions/{session_id}/result
        FUTURECOMPLETE_JOB_CANCEL_PATH_TEMPLATE: /v1/sessions/{session_id}/cancel
